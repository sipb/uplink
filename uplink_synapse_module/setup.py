from setuptools import setup

# TODO: migrate to pyproject.toml and fill out
# TODO: properly define module, seems like pip cannot use it?
# Workaround, copying to working dir /var/lib/matrix-synapse
# Seemed to work

setup()